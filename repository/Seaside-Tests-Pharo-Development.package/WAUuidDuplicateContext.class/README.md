I keep track of seen UUIDs and versions.

Instance Variables:
	seen	<Dictionary<UUID,MCVersionInfo>>
	duplicates	<UUID<Set<MCVersionInfo>>>
		
seen
	all UUIDs seen so far
		
duplicates
	all UUIDs that are used by multiple versions