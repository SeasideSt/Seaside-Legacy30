ancestry
parents
	"WAApplication makes use of the serverHostname, serverPath, serverPort, serverProtocol attributes so we depend on their configuration for convenience."
	^ Array with: WAGlobalConfiguration instance