streaming
binary
	"put the response stream into binary mode. For buffered response, 
	 replace the contentStream with a ByteArray based stream."

	contentsStream := WAPlatform current readWriteByteStream